{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["PUBLIC_API_URL", "PUBLIC_MAGIC_KEY", "NODE_ENV"],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lint": {}
  }
}
