{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["PUBLIC_MAGIC_KEY"],
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lint": {}
  }
}
