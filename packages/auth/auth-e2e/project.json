{
  "name": "auth-e2e",
  "$schema": "../../../node_modules/nx/schemas/project.json",
  "projectType": "library",

  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm vitest run",
        "cwd": "packages/auth/auth-e2e"
      },
      "dependsOn": ["spec:build"]
    }
  }
}
