{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",

  "name": "spec-lib",
  "projectType": "library",

  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "{projectRoot}",
        "command": "alloy"
      }
    }
  }
}
